#include<stdio.h>
#include<string.h>
int main()
{
char str[50];
int i;
printf("\nEnter string\n");
scanf("%s",str);

printf("%s",str);

for(i=0;i<=strlen(str);i++)
{
if(str[i]>=65&&str[i]<=90)
str[i]=str[i]+32;
}
printf("\nthe converted string is=%s\n,str");
 return 0;
}
