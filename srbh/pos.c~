#include<stdio.h>
#include<string.h>
int main()		
{
int i,n=1;
char str[100],m;

printf("\nENTER THE STRING\n");
gets(str);

printf("\nENTER THE CHARACTER TO SEARCH\n");
scanf("%s",&m);


for(i=0;str[i]!='\0';i++,n++)

if
  (m==str[i])

printf("\nposition of character is:%d\n",n);

return 0;
}
