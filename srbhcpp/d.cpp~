#include <cstdio>
using namespace std;
 
int main()
{
   int a[50], position, i, n;
   printf("Enter number of elements in array\n");
   scanf("%d", &n);
   printf("Enter %d elements\n", n);
   for (i=0;i<n;i++)
   scanf("%d", &a[i]);
   printf("Enter the element location to delete\n");
   scanf("%d",&position);
   for( i= position-1 ;i<n-1;i++ )
         a[i] = a[i+1];
   printf("Array after deletion of element is\n");
 
      for( i = 0 ; i< n - 1 ; i++ )
         printf("%d\n", a[i]);
   
 
   return 0;
}



